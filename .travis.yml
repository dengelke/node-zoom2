language: node_js
node_js:
  - "0.10"
  - "0.12"
  - "4"
  - "6"
  - "8"
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y yaz
  - sudo apt-get install -y libyaz5-dev
  - yaz-config --libs
  - yaz-config --version
  - yaz-config --prefix
after_success:
- npm run coveralls